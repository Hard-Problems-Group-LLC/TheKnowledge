Repository Maintenance Completed Tasks

Record completed maintenance work for TheKnowledge here. Keep
`templates/project-management/completed-tasks.txt` as starter material unless
the template wording or examples are being revised.

*
  2026-05-02T16:20:35-07:00 - Implemented the approved project-root
  `.local/`, TheKnowledge runtime-isolation, and consuming-project
  validation/wrapper proposals by adding
  `scripts/ensure_theknowledge_tool_runtime.py`, wiring direct-checkout
  timeout and quality-gate runs through a checkout-local Python 3.12 tools
  runtime under `.local/theknowledge-tool-runtime/`, fixing the base-
  interpreter resolver so bare Python 3.12+ can bootstrap the managed tools
  runtime, teaching consuming-project helpers to resolve vendored wrapper
  paths safely, and falling back from Git-private cache paths to `.cache/`
  when no usable Git directory exists. Updated the managed starter and docs
  to refresh a `.gitignore` block for `.local/`, local restricted-name
  inputs, and proactive Codex artifact ignores, documented
  `.local/ai-local-notes.*` as local-only policy inputs, promoted the three
  implemented proposals to `approved/`, and recorded the resolved imported
  ECR filenames in `internal/overrides/proposals/accepted-ecrs-list.md`.

*
  2026-05-02T15:50:17-07:00 - Added approved proposal
  `codex-wrangler-awareness-and-proactive-ignore-baseline.md` so
  TheKnowledge now records codex-wrangler awareness and proactive ignore
  coverage for known local Codex artifacts as baseline policy for direct
  checkouts and consuming-project starter material.

*
  2026-05-02T01:05:09-07:00 - Deconflicted the raw imported ECR intake by
  adding a deconfliction map, promoting the overlapping imported topics into
  top-level under-review proposals, and explicitly linking the remaining
  bootstrap and feedback-branch proposals to their imported intake sources.
  Expanded the managed ECR guidance from a flat `ECRs/TheKnowledge/`
  holding area to a status-based `open/`, `in-progress/`, and `closed/`
  lifecycle, aligned the canonical docs plus starter ECR scaffolding with
  that lifecycle, and updated the starter/doc-wiring tests to cover the new
  structure.

*
  2026-04-29T10:02:39-07:00 - Rebuilt the direct-checkout Python 3.12 tool
  runtime under `.local/theknowledge-tool-runtime/`, added `.local/` to
  `.gitignore`, created `.local/bin/theknowledge-python-tools` as a local
  wrapper path, removed the earlier `.git/`-local runtime, and drafted a new
  under-review ECR requesting that TheKnowledge's initial setup guidance
  standardize project-root `.local/` for untracked operator state and local
  tooling.

*
  2026-04-23T09:05:24-07:00 - Added a new stock performance/profiling knack,
  now placed under `knacks/performance/profiling/` as
  `WebPageLoadAndRenderProfiling.knack.md`, covering how consuming projects
  should build Playwright-backed utilities that combine browser timings,
  project-owned readiness markers, and post-render quiet windows to measure
  when slow web routes are structurally complete, semantically ready, and
  visually settled. Added the performance/profiling README scaffold,
  extended the knack scaffold test coverage, and revalidated the changed
  knack documents.

*
  2026-04-22T12:20:20-07:00 - Normalized proposal records and generated
  proposal starter files to Markdown, renamed the proposal-format standard to
  `standards-and-practices/docs/format-for-proposals.md`, updated the
  documentation profile to prefer Markdown for prose records and generated
  guidance, documented `ACP` as the add/commit/push shortcut for direct
  TheKnowledge maintenance and consuming projects, and revalidated the
  required check sequence with `127` passing tests.

*
  2026-04-01T01:42:16-07:00 - Corrected the managed install-policy contract
  after review so `install.sh` now defaults to a user-local standard install,
  development mode is explicit via `--mode dev`, system installs require
  `--system` under root, `sudo` without `--system` is rejected, and direct
  root use without `--system` requires confirmation. Updated the managed
  bootstrap code, starter docs/templates/specs, and added entrypoint
  regression tests for the privilege/mode matrix. Revalidated the full
  required TheKnowledge tool sequence with `127` passing tests.

*
  2026-03-31T20:48:26-07:00 - Added the approved follow-up ECR for
  interpreter-environment precedence and project-bound developer launchers,
  documented the broader cross-language environment-selection standard in
  `standards-and-practices/docs/Interpreters-and-Package-Management-Environments.md`,
  connected the Python bootstrap docs to that standard, and revalidated the
  relevant guideline/SOP doc checks plus entropy coverage after the updates.

*
  2026-03-31T20:08:27-07:00 - Adopted the approved converged managed
  bootstrap design on `trunk` by making `install.sh` plus
  `scripts/install-stage-2.py` the canonical POSIX starter, keeping
  `bootstrap.sh`, `bootstrap-stage2.py`, and
  `scripts/install_prerequisites.sh` as compatibility wrappers, switching the
  default runtime model to `pyenv` plus repo-local `.venv`, documenting the
  shared development/standard/venv-only install contract and optional
  `scripts/install_project.py` hook, fixing the
  stale managed-refresh import bug and the `dev_setup.py` helper-path
  regression, and revalidating the full required suite with `122` passing
  tests plus the explicit Black/Ruff passes on the new untracked starter
  files.

*
  2026-03-31T16:07:22-07:00 - Tightened the standardized Git commit
  workflow so it now rejects commits without an explicit author/committer
  identity, documented the repository-wide rule to never infer email
  addresses from history, hostnames, or network overlays, propagated that
  guidance into the managed starter docs, and revalidated the full required
  quality gate with `115` passing tests.

*
  2026-03-31T15:38:05-07:00 - Drafted two new under-review ECRs on `trunk`:
  one to replace the current install-prerequisites bootstrap with a minimal
  `install.sh` plus Python stage-2, user-scoped `pyenv`, repo-local
  `.python-version` and `.venv`, and mandatory `direnv`; and one to replace
  the old `Feedback` side branch with a clearer `feedback/ECRs` intake
  branch for structured cross-project proposals. While landing that intake,
  also fixed `scripts/validate_knacks.py` so `.git/...` cache paths resolve
  through the real git dir and the standard quality gate works in active
  submodule-style checkouts.

*

  2026-03-30T14:45:54-07:00 - Implemented the imported external-project
  TheKnowledge ECR set by strengthening the repository documentation
  standard, adding direct-checkout and starter `bootstrap.sh`,
  `bootstrap-stage2.py`, `python-environments.json`, `.python-version`, and
  `set-context*.sh` files plus `ECRs/TheKnowledge/` scaffolding, teaching
  the timeout wrapper to reuse its launching interpreter for non-Black
  tools, updating the managed specs/templates/docs, and revalidating the
  full required check suite with `112` passing tests.

*

  2026-03-30T12:13:45-07:00 - Imported an external project's
  `ECRs/TheKnowledge/` bundle unchanged under
  `internal/overrides/proposals/under-review/imported-ecrs/` so
  the upstream requests now live inside TheKnowledge's reviewable proposal
  state without modifying the source files in the consuming project.

*

  2026-03-27T12:38:45-07:00 - Executed the approved placement-driven Black
  validation and runtime-profile proposal by adding a managed
  `tool_validation_profiles.json` registry plus helper module, teaching the
  Black timeout wrapper to use Git-first profile discovery and managed
  runtime selection, splitting bootstrap entry points from the steady-state
  Python tool runtime in both direct-checkout and starter setup, refreshing
  the downstream installation and drift-refresh docs, adding regression
  coverage for the new managed files and helper behavior, and completing a
  deliberate repository-wide Black normalization pass before the full
  required validation suite passed with `105` tests.

*

  2026-03-27T00:34:41-07:00 - Filed a new open bug for the missing
  bootstrap-versus-steady-state Python runtime policy, drafted an under-
  review proposal for placement-driven Black validation and runtime
  profiles, and tied that proposal explicitly to the existing starter,
  sandbox, and full-scope Black spillover bug set so the issue can be
  resolved as one coherent design problem.

*

  2026-03-26T10:21:08-07:00 - Added a new stock `knacks/debugging/` area
  plus `HighLevelDebugging.knack.md`, drafted a language-neutral
  AI-assisted debugging guide at the 1,250-word target, wired the new area
  into the knack index and scaffold coverage, ran `knack_check`, `ruff`,
  `compileall`, `entropy_check`, `entropy_tripwire_verify`, and the full
  `pytest` suite successfully under a temporary Python 3.12 environment. The
  separate Black spillover is now resolved by the later placement-driven
  Black/runtime-profile implementation and deliberate repository-wide
  normalization pass recorded above.

*

  2026-03-25T21:32:26-07:00 - Replaced the temporary Codex-specific entropy
  exclusions with generic Git-ignore-aware entropy handling, added
  `--even-gitignored` across the scanner, harness, and tripwire verifier,
  aligned the cached quality-gate entropy scope with the same rule, updated
  the repo-local Codex isolation and entropy-tripwire specifications, and
  revalidated the full required check suite with `97` passing tests.

*

  2026-03-25T21:01:11-07:00 - Broadened the Codex sandbox Black fallback so
  `scripts/run_tool_with_timeout.py` and
  `scripts/run_quality_gate_cached.py` both route matched sandbox runs
  through registry-driven one-file-at-a-time timeout-harness execution,
  added shared execution-constraint lookup helpers, documented the stronger
  Black-only rule, excluded local Codex operator state from entropy and
  workspace-copy helpers, finished the repo-local Codex CLI isolation
  wrapper/spec/test work, and revalidated the full required check suite with
  `93` passing tests.

*

  2026-03-25T17:41:41-07:00 - Added new stock `knacks/licenses/` and
  `knacks/auditing/` areas, drafted a practical software-license library
  covering the top licenses of current concern with emphasis on
  incompatibilities and disallowed use cases, added an SBoM generation and
  auditing knack, refreshed the knack index, extended scaffold coverage, and
  revalidated the full required check suite plus `knack_check` with `83`
  passing tests.
*

  2026-03-25T16:52:22-07:00 - Added first-class submodule-aware helper
  tooling for consuming projects via
  `scripts/update_theknowledge_submodule.py`,
  `scripts/send_theknowledge_feedback.py`, and shared Git-dir-safe support
  logic; documented helper-first update/adopt and Feedback flows with an
  explicit read-only push caveat; expanded the submodule workflow
  specification plus downstream guidance; and added real submodule
  regression coverage before the final required validation suite passed with
  `83` passing tests.

*

  2026-03-25T16:45:37-07:00 - Replaced the hardcoded Codex-sandbox trigger
  with a managed repo-root `tool_execution_constraints.json` registry,
  taught `scripts/run_quality_gate_cached.py` to follow that registry for the
  narrow Black serial fallback, installed the registry into consuming
  projects through `scripts/initial_setup.py`, broadened the managed drift
  report and safe refresh command so downstream updates pick the file up, and
  added compact `AGENTS.md` critical-context guidance plus regression
  coverage before the full required validation suite passed with `79`
  passing tests.

*

  2026-03-25T15:40:42-07:00 - Implemented the remaining helper-behavior work
  from the approved serial file-safe static-analysis proposal by teaching the
  timeout wrapper to replace Black's default scope with explicit paths,
  teaching the cached quality gate to serialize Black one file at a time when
  `CODEX_SANDBOX_NETWORK_DISABLED=1` is present, adding regression coverage
  for the new wrapper and sandbox-aware dispatch behavior, and revalidating
  the full required check suite with `77` passing tests.

*

  2026-03-25T15:15:56-07:00 - Resolved the outstanding `Feedback` intake by
  closing the underdocumented active-submodule reporting bug, approving the
  incoming proposals, adding specifications for TheKnowledge submodule
  workflows and the Codex-sandbox static-analysis fallback, updating the
  direct-checkout and downstream docs to make those workflows explicit, and
  keeping the upstream sandbox wakeup defect open with a documented
  one-file-at-a-time mitigation.

*

  2026-03-25T14:28:19-07:00 - Fixed the cached quality-gate helper so a
  `.git/...` cache path resolves through the real git dir in submodule and
  separate-git-dir checkouts, added regression coverage for the cache-path
  normalization, and closed the matching submodule workflow bug after the
  full required validation suite passed.

*

  2026-03-25T14:10:59-07:00 - Reviewed the work merged in from
  `Feedback`, separated the landed proposal from the still-open bug
  reports, and backlogged the two repo-side corrective actions for the
  submodule cache-path failure and the underdocumented `Feedback`
  submodule workflow.

*

  2026-03-25T05:34:34-07:00 - Added a new `knacks/sandboxing/`
  category with a `bubblewrap.knack.md` write-up covering bubblewrap's
  mental model, practical command patterns, and high-value failure
  analysis; updated the knack scaffold test; and revalidated the full
  required check suite plus `knack_check`.

*

  2026-03-25T04:53:39-07:00 - Clarified the knack API naming standard so
  language-qualified files use patterns such as `curses.api.C.knack.md`
  and `directx.api.rendering.CPP.knack.md`, added C, Python, and C++
  API knacks for the terminal curses and ncurses materials, refreshed
  the terminal indexes, and revalidated the full required check suite.

*

  2026-03-25T02:40:26-07:00 - Drafted the initial terminal knack library
  for ANSI control sequences, VT100 compatibility, xterm-family emulator
  behavior, the generic curses model, and ncurses-specific guidance;
  refreshed the terminal knack README; and revalidated the full required
  check suite plus `knack_check`.

*

  2026-03-25T01:37:03-07:00 - Added a detailed open bug report for the local
  `bubblewrap --argv0` sandbox incompatibility, recorded the summary in
  `known-bugs.txt`, and documented evidence, options, and recommended
  next steps for further investigation.

*

  2026-03-25T01:37:03-07:00 - Approved the inline AI conversation timestamp
  proposal, added a specification plus direct-checkout and downstream
  guidance for timestamped intermediary development updates, and
  extended the doc-wiring tests so workflow profiling stays inherited by
  generated projects.

*

  2026-03-24T23:52:46-07:00 - Made review-first staging the default workflow for
  TheKnowledge and consuming projects, updated the standardized commit
  helper to prompt for optional review before any `git add`, require
  `git diff --cached` before committing, and documented Meld as the
  default visual review path plus its official install page across the
  repository and templates.

*

  2026-03-24T22:40:40-07:00 - Added `git diff`-before-staging guidance
  to TheKnowledge and consumer workflow docs, taught the standardized
  commit helper to show diffs before its own `git add` steps, added an
  `AGENTS.md` drift-report helper plus submodule-update guidance for
  consuming projects, and revalidated the full required check suite.


*

  2026-03-24T21:12:51-07:00 - Created a local `Feedback` branch and
  documented that it is only for TheKnowledge feedback discovered while
  working primarily in consuming projects, while direct TheKnowledge
  maintenance continues to use the normal internal trees on `trunk`.


*

  2026-03-24T20:54:31-07:00 - Added lightweight knack validation with a
  changed-file hash cache, wired the check into the cached quality gate,
  documented standalone-versus-submodule knack roots plus collision handling,
  and updated AGENTS templates so consuming projects know how to validate
  top-level knacks.


*

  2026-03-24T20:32:28-07:00 - Defined knack authoring guidance for overview
  and API word-count targets, a 5,000-word per-file ceiling, multi-file
  naming and directory patterns, and durable-source expectations, then added
  tests so the new guidance stays wired into the repository.


*

  2026-03-24T20:01:33-07:00 - Added a public `knacks/` scaffold with
  placeholder README files for `UI/terminal/`, documented the new
  `.knack.md` convention in a specification and the top-level README, added
  structural test coverage, and backlogged the first terminal knack
  documents for later drafting.

*

  2026-03-20T00:35:52-07:00 - Added required proposal status directories
  (`approved/`, `rejected/`, `deferred/`, and `under-review/`) plus bug
  status directories (`open/`, `in-progress/`, and `closed/`) across the
  standards, starter templates, repository-local override records, and setup
  coverage so TheKnowledge and downstream installs share the same status-tree
  model.

*

  2026-03-13T09:08:05Z - Replaced `closed-bugs.txt` with
  `resolved-bugs.txt` across TheKnowledge's bug lifecycle guidance, added a
  resolved-bug record specification plus template and override files that
  require `Root cause:` and `Resolution:` notes, and updated scaffold tests
  so initial setup installs the new file instead of the old closed-bugs
  name.

*

  2026-03-13T00:46:38-07:00 - Removed consumer-specific names from generic
  sanitization tests so the public test suite stays aligned with
  distribution-neutral naming requirements.

*

  2026-03-10T13:56:48-07:00 - Expanded `README.md` with concrete commands for
  adding TheKnowledge as a submodule in either an existing project or a new
  repository, running the initializer, and cloning or initializing submodules
  correctly in downstream checkouts.

*

  2026-03-10T13:29:19-07:00 - Split the guideline antipattern material into
  separate UI and development documents, added a pending-commit change queue
  policy with template and internal state files, and updated the standardized
  commit helper plus workflow documentation so queued summaries become commit
  body text and clear after a successful local commit.

*

  2026-03-10T20:07:08+00:00 - Added a `guidelines/` document and supporting
  specification for UI complexity limits, distilling Miller, Cowan, current
  platform guidance, and a brief rate-distortion justification into a
  practical 7-to-9-item recommendation with a references section.

*

  2026-03-10T19:50:40+00:00 - Added a generic `internal-web-services`
  technique, added reusable `tailscale` and `caddy` technology SOPs, and
  imported sanitized internal-service utility scripts for Tailscale-managed
  HTTPS certificates, Caddy reverse-proxy configuration, and Podman and
  tailnet health checks.

*

  2026-03-10T19:29:34+00:00 - Added reusable SOP material for language,
  technology, and technique guidance, including a generic `new-web-service`
  standard for Python 3.12, FastAPI, rootless Podman, React, Vite, and a
  BerkeleyDB-to-PostgreSQL storage transition boundary, and added tests that
  keep the SOP subtree free of project-specific naming.

*

  2026-03-10T18:38:59+00:00 - Hardened `scripts/initial-setup.py` so it
  removes all preexisting managed `AGENTS.md` header and footer sections
  before rewriting the file, updated template documentation, and added a
  regression test that normalizes an accidentally duplicated managed state.

*

  2026-03-10T18:36:19+00:00 - Added an idempotency test for
  `scripts/initial-setup.py` so rerunning the installer does not duplicate the
  managed `AGENTS.md` header or footer blocks.

*

  2026-03-10T18:10:11+00:00 - Added managed `templates/AGENTS-header.md` and
  `templates/AGENTS-footer.md` support, updated `scripts/initial-setup.py` to
  create or wrap consumer `AGENTS.md` files, and expanded installer tests to
  cover the managed TheKnowledge guidance scaffold.

*

  2026-03-10T17:16:58+00:00 - Restructured the repository into
  `standards-and-practices/`, `templates/`, and `internal/`; added
  `scripts/initial-setup.py`; and updated tests so consuming projects can
  install starter project-management files into their own roots while
  TheKnowledge keeps its own maintenance state internal.

*

  2026-03-10T16:46:41+00:00 - Added `internal/overrides/` mirror records,
  override guidance, and a structural test so repository maintenance can be
  tracked without populating the starter templates.
