# Internal Material

Material in and below `internal/` is for development and maintenance of the
submodule itself.

Keep this subtree for repository-local state, notes, and records that should
not populate the clean starter material exposed under `templates/`.

For project-management live state in this repository, use
`internal/overrides/README.txt`, including the repository-local proposal and
bug status subdirectories.
