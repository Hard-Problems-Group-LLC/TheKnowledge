# Performance

Load this area when a task is about measuring or explaining runtime behavior,
latency, rendering cost, or resource consumption rather than implementing a
feature directly.

Current stock subareas:

- `profiling/`: guidance for building repeatable measurement and profiling
  utilities for web routes and other performance-sensitive paths.
