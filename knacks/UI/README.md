# UI Knacks

This area holds reusable knack documents for user-interface work.

Subdirectories may split UI knowledge by medium or platform. The initial
public scaffold reserves `terminal/` for terminal protocols, emulator
behavior, and terminal library guidance.
